<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--主页-->
    <string name="activity_main_nav_menu_open">打开侧滑菜单</string>
    <string name="activity_main_nav_menu_close">关闭侧滑菜单</string>

    <style name="TransparentTheme" parent="AppTheme"></style>

    <style name="MyCheckBox_Play" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@drawable/ic_play_pause_selector</item>
    </style>
</resources>
